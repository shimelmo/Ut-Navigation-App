#Notes
#-Make sure you use ctr s to save any files before you try to push them. Nothing will be pushed unless you save dummy 




Git Commands
Git add . #adds all changes to be staged
git commit -m "Your Message here" #commits your code into the staging area
Git push origin main #pushes your code to git hub

git pull #Pulls new stuff from git hub 

